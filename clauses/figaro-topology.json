{
    "clauseId": "figaro-topology",
    "version": 1,
    "title": "Order topology",
    "description": "Records the topology of an order within a process. The on-chain kernel stores only per-process accumulator state; parent-order edges are carried here and committed via agreementHash.",
    "categories": ["topology"],
    "fields": [
        {
            "name": "topologyMode",
            "type": "enum",
            "values": ["root", "explicit", "linear-fallback"],
            "required": true,
            "description": "How this order's parents are determined."
        },
        {
            "name": "parentOrderHashes",
            "type": "array",
            "required": true,
            "minItems": 0,
            "items": {
                "type": "string",
                "format": "bytes32-hex",
                "name": "parent",
                "required": true
            },
            "description": "Parent order hashes (bytes32 hex). Empty for root orders."
        }
    ],
    "block": {
        "tier": "agreement-only",
        "article": "identity",
        "structural": true,
        "mechanismKinds": ["core"],
        "moduleIds": ["process-graph", "order-node"]
    }
}
