# estate-snapshots

One directory per capture date, written by the Estate Snapshot workflow (`.github/workflows/estate-snapshot.yml`): the GitHub traffic captures, the Cloudflare daily request series for the site, and the npm downloads of the SDK. The weekly digest routine reads this branch.
