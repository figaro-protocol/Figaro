
CONSTANTS
    STAGE_COUNT = 3
    Accounts = {"alice", "bob"}
    Submitter = "submitter"
    NonSubmitters = {"alice", "bob", "mallory"}
    MaxTime = 4

SPECIFICATION Spec

\* Deadlock check disabled: a state where every stage is fully claimed
\* and time has hit MaxTime is a legitimate end state.
CHECK_DEADLOCK FALSE

INVARIANTS
    TypeOK
    Inv_SubmitterImmutable
    Inv_UnlockTimeImmutable
    Inv_RootInRange
    Inv_ClaimedTyped
    Inv_ClaimImpliesRootSet
    Inv_ClaimImpliesUnlocked
    Inv_TotalAllocatedLockedWithRoot
    Inv_StageIndexBounded
