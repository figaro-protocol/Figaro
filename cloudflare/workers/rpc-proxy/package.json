{
    "name": "@figaro/cf-rpc-proxy",
    "version": "0.1.0",
    "private": true,
    "description": "Figaro beta RPC proxy — JSON-RPC method allowlist + address allowlist + Anvil container backing.",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "lint": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241218.0",
        "typescript": "^5.5.0",
        "wrangler": "^3.95.0"
    }
}
