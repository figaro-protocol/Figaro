---
name: Assembly proposal
about: Propose a new assembly composing clauses into a reusable institution
title: ""
labels: assembly-proposal
---

<!--
  An assembly is a composition of clauses + topology, not a code change —
  registering it on AssemblyRegistry is permissionless and requires no
  maintainer approval. This issue is for discussion before you register, not
  a gate you need cleared to register. See assemblies/README.md for the
  reference-assembly model.
-->

## What it expresses

<!-- The real-world composition this assembly represents: the roles, the
     order topology (who buys from whom), and what makes it worth reusing. -->

## Composition sketch

<!-- A sketch of the orders and the clauses each one carries, or a link to a
     draft AssemblyTemplate (designer export or hand-authored JSON). Doesn't
     need to be final. -->

## Which existing clauses it composes with

<!-- The clauses (clauses/*.json) each order in the assembly carries,
     including the mandatory ones (figaro-commerce, figaro-topology). -->

## Staked-registration acknowledgment

<!-- Registration on AssemblyRegistry requires a live ETH stake tied to the
     registering wallet (see docs/CONTRACTS.md § RPGF and
     script/DeployMainnet.s.sol for current sizing) — confirm you understand
     registration is permissionless, first-write-wins by compositionHash, and
     immutable, and that usage/reward accrual requires the stake to stay
     live. -->
