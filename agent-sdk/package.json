{
  "name": "@figaro/agent-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Programmatic access to the operator's repo-building agents (kernel-reviewer, clause-lockstep, marketing-author, …) for non-Claude-Code runtimes. Parses .claude/agents/*.md (canonical source) and exposes structured AgentDefinition objects. Excludes the public ecosystem agents in sdk/ecosystem-agents/.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
