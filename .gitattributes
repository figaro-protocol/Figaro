# What GitHub's language statistics leave out. Generated output is committed
# so the site and the SDK ship it, but it is not code anyone wrote here:
# the typedoc API reference (frontend/public/sdk-api, regenerated by
# `cd sdk && npm run docs`, gated in sdk-ci) and the ABI bundle emitted from
# the compiled contracts (scripts/emit-abi-bundle.sh, gated in pre-commit).
# Marking them keeps the bar on the hand-written tree: TypeScript, Solidity,
# Rust, the formal models.
frontend/public/sdk-api/** linguist-generated
sdk/src/abis.ts linguist-generated
