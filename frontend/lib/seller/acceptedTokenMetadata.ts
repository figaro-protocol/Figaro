/**
 * lib/seller/acceptedTokenMetadata.ts
 *
 * `AcceptedTokenMetadata` — the tokens a seller accepts for settlement —
 * is part of the seller-profile document shape owned by `@figaro/sdk`.
 * This module re-exports it so existing `@/lib/seller/...` call sites keep
 * working; add nothing here.
 */

export type { AcceptedTokenMetadata } from "@figaro/sdk";
